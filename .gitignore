## .NET build artifacts
bin/
obj/
*.user
*.suo
.vs/

## SER output files (note: a bare *.ser would also match the SerCapture.Ser project
## folder on case-insensitive Windows, so scope it to generated locations only)
/out/
**/bin/**/*.ser
